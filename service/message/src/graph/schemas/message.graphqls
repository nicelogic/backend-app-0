extend type Query {
  getMessages(chatId: String!): [Message!]
}

extend type Mutation {
  createMessage(chatId: String!, message: String!): Message!
}

type Subscription {
  newMessageReceived(userId: String!): NewChatMessage!
}

type NewChatMessage{
  id: ID!
  message: Message!
}
