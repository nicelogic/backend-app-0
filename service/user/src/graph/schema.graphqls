# GraphQL schema example
#
# https://gqlgen.com/getting-started/


type User {
  id: ID! @goField(name: "user_id")
  name: String!
  signature: String!
}

type Query {
  me: User!
  user(idOrName: String!): User!
}

scalar Map

type Mutation {
  updateUser(changes: Map!): User!
}
