
apiVersion: v1
kind: ConfigMap
metadata:
  name: config
  namespace: app-0
data:
  config.yml: |+
    cassandra-auth-url: http://cassandra-cluster-env0-dc1-stargate-service.k8ssandra-operator:8081/v1/auth
    cassandra-graphql-url: http://cassandra-cluster-env0-dc1-stargate-service.k8ssandra-operator:8080

